# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://node128.codingbc.com:7878/guy_sentiment}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Nov2017890#}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000

# AWS Configuration (move to environment variables!)
amazon.aws.accesskey=${AWS_ACCESS_KEY:AKIA6PS436XZ3KBU6GGT}
amazon.aws.secretkey=${AWS_SECRET_KEY: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}
bucket.url=${AWS_BUCKET_URL:files.handson.academy}

# OpenAI Configuration (move to environment variables!)
openai.api.key=${OPENAI_API_KEY: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}

# Server Configuration
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.info.env.enabled=true

# Logging Configuration
logging.level.root=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
logging.level.your.package.name=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Spring Security (if you're using it)
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:changeme}

# Cache Configuration
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterAccess=600s

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Compression
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain

# Session Configuration
server.servlet.session.timeout=30m